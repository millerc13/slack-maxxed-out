# Copy this to .env and fill in your values
# DO NOT commit .env to git!

# Slack Webhook URLs for notifications
# Create incoming webhooks at: https://api.slack.com/apps → Your App → Incoming Webhooks

# Default webhook URL (used as fallback)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Optional: Use separate webhooks for different alert types/channels
SLACK_WEBHOOK_ABANDONED_CHECKOUT=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
SLACK_WEBHOOK_PURCHASE=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
